naive bayes
knn
decision tree messing
ensemble of all classifier methods

make spreadsheet where rows are model and columns are scoring metrics: Overall, recall, precision, F1, and AUC
Want recall around 75-80%

Add sensitivity
Use oversampling and bagging instead of SMOTE
